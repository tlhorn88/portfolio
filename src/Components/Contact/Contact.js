import './Contact.css';
import SectionTitle from '../SectionTitle/SectionTitle';

function Contact() {
    return (
        <div class="contact">
            <SectionTitle title = "Contact"/>
        </div>
    )
}

export default Contact;